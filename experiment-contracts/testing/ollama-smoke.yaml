# Test-only software runtime config.
# This does NOT replace the canonical Llama 3.2 / llama.cpp tutor baseline.

schema_version: "0.1-test"

runtime:
  backend: "ollama"
  endpoint: "http://127.0.0.1:11434"
  model: "qwen2.5:0.5b"

software:
  temperature: 0.0
  max_output_tokens: 128

prompt:
  system_prompt: "prompts/tutor_system.txt"
  user_prompt: "Explain cache memory in one short paragraph."

measurement:
  repetitions: 1
