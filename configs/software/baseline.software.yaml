# Draft AI Tutor Software Baseline Configuration
# Confirmed architecture: 1B quantized Ollama model with integrated RAG pipeline.
# Concrete parameter values pending team definition (runtime_ready: false).

status: draft
runtime_ready: false
description: "Draft AI Tutor software baseline configuration. Confirmed architecture: 1B quantized Ollama model with integrated RAG pipeline. Concrete parameter values pending team definition."

runtime: ollama
parameter_count: "1B"

rag:
  enabled: true

active_knobs:
  temperature:
    status: pending_definition
    unit: unitless
    intended_purpose: "Sampling temperature for answer generation"
  chunk_overlap:
    status: pending_definition
    unit: tokens
    intended_purpose: "Token overlap between adjacent document chunks in RAG indexing"
  similarity_metric:
    status: pending_definition
    intended_purpose: "Vector distance metric used for RAG embedding retrieval"
  quantization:
    status: pending_definition
    intended_purpose: "Weight quantization format for 1B Ollama model"

metrics:
  application:
    answer_quality: null
    latency_ms: null

unresolved_fields:
  - "exact 1B Ollama model artifact"
  - "temperature baseline and legal values"
  - "chunk_overlap baseline and legal values"
  - "similarity_metric baseline and legal values"
  - "quantization baseline and legal values"
  - "embedding model"
  - "chunk size"
  - "retrieval top-k"
  - "max context tokens"
  - "max new tokens"
  - "batch size"
  - "seed"
  - "exact RAG corpus"
